services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: streetman
      MYSQL_USER: streetman
      MYSQL_PASSWORD: streetman
    ports:
      - "3307:3306"
    volumes:
      - dbdata:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-prootpass"]
      interval: 5s
      timeout: 5s
      retries: 30

  app:
    build: .
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      APP_KEY: "base64:2mQUsSrB5jhtEvHMVWTrVtyS4dL1p2dsSAFqhMU0yiU="
      APP_URL: http://localhost:8000
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: streetman
      DB_USERNAME: streetman
      DB_PASSWORD: streetman
      QUEUE_CONNECTION: sync
      SESSION_DRIVER: database
      CACHE_STORE: database
      FILESYSTEM_DISK: local
      ADMIN_EMAIL: admin@streetman.com
      ADMIN_PASSWORD: password
      RUN_MIGRATIONS: "true"
    ports:
      - "8000:8000"
    command: ["php", "artisan", "serve", "--host=0.0.0.0", "--port=8000"]

volumes:
  dbdata: